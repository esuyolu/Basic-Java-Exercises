public class MathUtil {
    public static int min(int oldVal, int newVal)
    {
        return newVal < oldVal ? newVal : oldVal;
    }

    public static int max(int oldVal, int newVal)
    {
        return newVal > oldVal ? newVal : oldVal;
    }
}
